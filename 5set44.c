#include<stdio.h>
void main()
{
int n,low,high;
printf("enter the number %d",n);
printf("enter the low value %d",low);
printf("enter the high value %d",high);
if((n-low)*(n-high)<=o)
{
printf("%d is in the range [%d %d]",n,low,high);
}
else
{
printf("%d is not in the range [%d %d]",n,low,high);
}
}
