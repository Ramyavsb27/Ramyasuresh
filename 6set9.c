#include<stdio.h>
void main()
{
  int max,x[10],i,n;
  printf("enter the elements");
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&x[i]);
   for(i=0;i<n;i++)
     max=x[0];
  if(mxa<x[i])
  {
    max=x[i];
  }
  printf("%d",max);
}
  
